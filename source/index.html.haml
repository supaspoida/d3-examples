%h1 d3 Examples

%dl
  - example_groups.sort.each do |type, examples|
    %dt= type.titleize
    %dd
      %ul
        = list_of examples.sort_by(&:name) do |example|
          = link_to example.name.titleize, example.path
